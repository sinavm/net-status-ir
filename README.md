# net-status-ir

داشبورد ساده برای بررسی دسترسی به چند سرویس عمومی (داکیومنت، گیتهاب، ویکی‌پدیا، کلاودفلر).

این پروژه کانفیگ تونل نمی‌سازد. فقط HTTP status را از runner گیتهاب ثبت می‌کند.

## اجرا

```bash
python3 check.py
```

خروجی: `status.json` و `index.html`
